import requests

resp = requests.get("https://pokeapi.co/api/v2/pokemon/")
print(resp.headers)

gen_pokemon = [generacion['generation']['name'] for generacion in data['generation']]
    print(f"Generacion: {gen_pokemon}")